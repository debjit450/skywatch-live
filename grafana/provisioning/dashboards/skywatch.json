{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "targets": [{ "expr": "skywatch_active_flights_total", "refId": "A" }],
      "title": "Active Flights",
      "type": "stat"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "targets": [{ "expr": "sum by (severity) (skywatch_anomalies_detected_total)", "refId": "A" }],
      "title": "Anomalies by Severity",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "targets": [{ "expr": "skywatch_websocket_connections", "refId": "A" }],
      "title": "WebSocket Connections",
      "type": "stat"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 4,
      "targets": [{ "expr": "histogram_quantile(0.95, sum(rate(skywatch_data_ingestion_latency_seconds_bucket[5m])) by (le))", "refId": "A" }],
      "title": "Ingestion Latency p95",
      "type": "timeseries"
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 5,
      "targets": [{ "expr": "sum(rate(skywatch_cache_hits_total[5m])) / clamp_min(sum(rate(skywatch_cache_hits_total[5m])) + sum(rate(skywatch_cache_misses_total[5m])), 1)", "refId": "A" }],
      "title": "Cache Hit Ratio",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["skywatch"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "SkyWatch Live Starter",
  "uid": "skywatch-live-starter",
  "version": 1
}
